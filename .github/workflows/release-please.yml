name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    uses: dryvist/.github/.github/workflows/_release-please.yml@main
