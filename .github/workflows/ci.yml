name: CI

on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [develop]

permissions:
  contents: read

jobs:
  nix-validate:
    name: Nix Validate
    uses: dryvist/.github/.github/workflows/_nix-validate.yml@main
    with:
      all_systems: false
